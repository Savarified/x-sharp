#include <stxs>;

clear;
string apology = "I'm sorry I told you I jerked it to sponoegeobob when I was 9..";
disp (apology);
int delay = 2;
wait (delay);
clear;

string text = "I love you so so much";
int iter = 64;
repeat (iter){
 disp (text)
};

<!>