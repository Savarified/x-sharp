#include <stxs>;

>/ my first xs script!;

string greeting = "Hello world!!";
disp (greeting);
wait (1);
clear;

int a = 0;
int count = 4;

repeat (count){
 disp ("a: "),
 disp (a),
 a += 2
};

disp ("impressed yet?");
>! even got comments?? he must be a genius!!;

<!>
osadksnDASFODSKFBGNV